# Development-only dependencies. NOT installed in the production Docker image.
# Install locally with: pip install -r requirements-dev.txt
#
# Kept separate from requirements.txt so the Railway container stays lean and
# doesn't ship pytest/pre-commit/virtualenv/etc. into a running trading bot.

# Testing
pytest==8.3.2
iniconfig==2.0.0
pluggy==1.5.0

# Pre-commit hooks
pre-commit==3.8.0
cfgv==3.4.0
identify==2.6.0
nodeenv==1.9.1
virtualenv==20.26.3
distlib==0.3.8
filelock==3.15.4

# Local build tooling
build==1.2.1
pyproject_hooks==1.1.0

# Unused in prod code — kept here instead of deleting outright in case a
# utility script starts depending on it. Remove if confirmed dead.
kubernetes==30.1.0

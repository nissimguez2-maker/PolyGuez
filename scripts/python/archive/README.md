# Archived one-off migration scripts

These Python scripts were used to apply specific Supabase schema changes in
the past. They have already been run against prod and are kept here for
historical reference only. Do not run again.

Canonical schema lives in `supabase/schema.sql`; incremental changes go in
`supabase/migrations/*.sql`.

# Troubleshooting

## Install issues
- Delete lockfile + reinstall if dependency tree is broken
- Re-run the package manager with a clean cache if needed

## Env issues
- Ensure required env vars are set
- Do not commit real API keys

## Runtime issues
- Start with the smallest example / minimal config
- Enable verbose logging where supported

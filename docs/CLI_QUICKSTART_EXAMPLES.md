# CLI quickstart examples

This document provides practical examples for using the Polymarket Agents
CLI, building on the short introduction in the main README.

Use it as a reference when you want to try out common commands or share
reproducible examples of CLI usage.

---

## 1. Basic invocation

From the repository root, the CLI is typically invoked as:

```bash
python scripts/python/cli.py <command> [options]

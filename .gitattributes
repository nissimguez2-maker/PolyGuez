# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Ensure Python files always use LF (Linux-style) line endings
*.py text eol=lf

# Explicitly declare documentation files as text
*.md text

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary

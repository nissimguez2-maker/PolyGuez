# PolyGuez — Live State

> Auto-refreshed by `.github/workflows/refresh-context.yml` on every push to `main`
> and daily at 06:00 UTC. For stable system docs, architecture, config, and
> standing rules, read [`SYSTEM.md`](SYSTEM.md). For Claude behavioural rules,
> read [`CLAUDE.md`](CLAUDE.md).

<!-- LIVE_STATE_BEGIN -->
## LIVE STATE (auto-refreshed)

_This block is machine-written by `.github/scripts/refresh_context.py`. Do not
edit manually. It will be overwritten on the next run of the
`refresh-context` GitHub Action. If the "Refreshed at" timestamp is more than
26 hours old, the auto-refresh is broken — flag it to Nessim rather than
acting on the numbers._

**Refreshed at:** _pending next action run_

For stable system docs see [`SYSTEM.md`](SYSTEM.md).

<!-- LIVE_STATE_END -->
